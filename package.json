{
  "name": "witchcraftmc-website",
  "private": true,
  "description": "WitchCraftMC wiki & landing site. JS test harness for /js/lib/* — no runtime dependencies.",
  "scripts": {
    "test": "node tests/placeholders.test.js"
  }
}
